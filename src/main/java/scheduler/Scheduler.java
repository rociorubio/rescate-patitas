package scheduler;

public class Scheduler {
  public static void main(String[] args){
 /*   // tengo la lista de asociaciones y genero recomendaciones para todas
    List<Asociacion> asociaciones = RepositorioAsociaciones.getAsociaciones();
    asociaciones.forEach(asociacion -> {
      GeneradorDeRecomendaciones generadorDeRecomendaciones = new GeneradorDeRecomendaciones();
      List<Recomendacion> recomendaciones = generadorDeRecomendaciones.generarRecomendaciones(asociacion);
      recomendaciones.forEach(recomendacion -> recomendacion.notificar());
    });
  */
    System.out.println("Enviando Recomendaciones...");
  }
}